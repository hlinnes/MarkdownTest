# FAQ

## Why is there a sidebar instead of tabs?

The Material theme shows a sidebar by default unless `navigation.tabs` is enabled.

## Where do files go?

- `mkdocs.yml` → project root
- Markdown files → `docs/`